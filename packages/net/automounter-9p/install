#!/bin/sh

. config/options

get_meta $1
mkdir -p $INSTALL/etc
cp $PACKAGES/$PKG_SECTION/$1/config/9p $INSTALL/etc

mkdir -p $INSTALL/usr/bin
cp $PACKAGES/$PKG_SECTION/$1/scripts/automount_9p $INSTALL/usr/bin
